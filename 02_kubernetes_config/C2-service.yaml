apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: dissertation-eval
  labels:
    app: nginx-microservice
spec:
  type: NodePort
  selector:
    app: nginx-microservice
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
